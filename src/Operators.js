/**
 * 
 */

var printer = console.log;
var val = 1;

val++;
printer(val);

val--;

printer(val);

val += 1;
printer(val);

val -= 1;
printer(val);

val *= 12;
printer(val);

val /= 5;
printer(val);

val %= 2;
printer(val);